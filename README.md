# Shared Auth Library

Reusable authentication and authorization library used across Go services.

## Features
- JWT token generation and validation
- RBAC middleware
- OAuth2 client helpers

## Usage
```go
import "github.com/BuildGuard-Test-Lab/shared-auth-lib/auth"
```
